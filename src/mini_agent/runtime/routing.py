"""Deprecated routing module retained as an import-safe architecture marker.

Strategy dispatch now happens directly in :mod:`mini_agent.runtime.runner` from
the configured ``RunnerSettings.strategy``; no automatic selector is present.
"""
